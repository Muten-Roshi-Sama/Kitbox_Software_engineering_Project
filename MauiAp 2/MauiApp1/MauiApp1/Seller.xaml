<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Seller"
             BackgroundColor="#C5FCEF"> 
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="LightGrayColor">#F0F0F0</Color>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout>
        <StackLayout Orientation="Horizontal" Padding="10" Spacing="10" BackgroundColor="#65BAA9">
            <Label Text="Id" WidthRequest="100" FontAttributes="Bold" TextColor="White"/>
            <Label Text="Reference" WidthRequest="100" FontAttributes="Bold" TextColor="White"/>
            <Label Text="DateC" WidthRequest="100" FontAttributes="Bold" TextColor="White"/>
            <Label Text="Description" WidthRequest="100" FontAttributes="Bold" TextColor="White"/>
            <Label Text="NameFile" WidthRequest="100" FontAttributes="Bold" TextColor="White"/>
        </StackLayout>
        <Line Stroke="black" X2="1000" />
        <ListView x:Name="MyListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <HorizontalStackLayout Padding="10" Spacing="10" HorizontalOptions="FillAndExpand">
                                <Label Text="{Binding Path=Id}" HorizontalOptions="FillAndExpand" WidthRequest="100"/>
                                <Label Text="{Binding Path=Reference}" HorizontalOptions="FillAndExpand" WidthRequest="100"/>
                                <Label Text="{Binding Path=DateC}" HorizontalOptions="FillAndExpand" WidthRequest="100"/>
                                <Label Text="{Binding Path=NameFile}" HorizontalOptions="FillAndExpand" WidthRequest="100"/>
                                <ImageButton Source="sort_down.png" ClassId="Button3" IsVisible="{Binding Path=infoSupOff}" WidthRequest="20" HeightRequest="20" />
                                
                                <Border Stroke="#000000"
                                        StrokeThickness="1"
                                        Background="#ffffff"
                                        Padding="1,1"
                                        HorizontalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20,20,20,20" />
                                    </Border.StrokeShape>
                                    <Label Text="Description" HorizontalOptions="FillAndExpand" VerticalOptions="Center">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="ShowDescription"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                </Border>
                            </HorizontalStackLayout>
                            <StackLayout IsVisible="{Binding Path=showDescr}">
                                <Label Text="{Binding Path=Description}" HorizontalOptions="FillAndExpand"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button x:Name="Disconnect" Margin="10,0,0,0" BackgroundColor="#4D8075" HeightRequest= "7" WidthRequest=" 200" Text="Disconnect" TextColor="#C5FCEF" Clicked="disconnect"></Button>
    </StackLayout>

</ContentPage>